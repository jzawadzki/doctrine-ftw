{% extends 'base.html.twig' %}

{% block body %}
    <h1>Brands</h1>
    <br />
<ul>
    {% for row in brands %}
        <li>{{ row.brand.name }} ({{ row.customer.name }})</li>
    {% endfor %}
</ul>
{% endblock %}
